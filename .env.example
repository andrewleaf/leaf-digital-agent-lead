# Database dialect: sqlite (default) or postgres.
DB_DIALECT=sqlite

# SQLite file path relative to the repo root (used when DB_DIALECT=sqlite).
SQLITE_PATH=data/localdraft.sqlite

# PostgreSQL connection string (required when DB_DIALECT=postgres).
# DATABASE_URL=postgres://user:password@localhost:5432/localdraft
